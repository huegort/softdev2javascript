var spalt =.5;
RangeGrid = function(range){
    this.range = range;
    this.grid = new Array((range*2)+1);

}
function init(range,grid){
    var dim = (range*2)+1;
    var difX= -range;
    var difY= -range;
    for (var row = 0; row<dim;row++){
        for(var col = 0; col <dim; col++){

            difX--
        }
        difY--
    }


}